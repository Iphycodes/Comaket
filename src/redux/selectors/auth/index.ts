// This file previously contained selectors that depended on non-existent exports
// (getAccounts, getApp, getConstants) from '@grc/services/auth'.
// All selectors were dead code (unused elsewhere in the codebase) and have been removed.
